const fs = require('fs'); let content = fs.readFileSync('client/src/pages/Home.tsx', 'utf8'); const blogStart = content.indexOf('            {/* -- Blogs Highlight -- */}'); const teachingsStart = content.indexOf('            {/* -- Teachings Highlight -- */}'); const blogCode = content.substring(blogStart, teachingsStart); content = content.substring(0, blogStart) + content.substring(teachingsStart); const booksStart = content.indexOf('            {/* -- Featured Books -- */}'); content = content.substring(0, booksStart) + blogCode + '\n' + content.substring(booksStart); fs.writeFileSync('client/src/pages/Home.tsx', content); console.log('Successfully moved Blog section');
